import 'package:flutter/material.dart';

class InfoApl extends StatelessWidget {
  const InfoApl({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Coming Soon!',
      ),
    );
  }
}
